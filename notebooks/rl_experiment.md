# RL Sandbox

This repository includes a replay-only RL sandbox under `app/rl/`.

- It is experimental only.
- It must not be wired into live or paper order decisions.
- It is intended for offline replay and feature research only.

Use `python scripts/rl_experiment.py` to exercise the stub environment.
